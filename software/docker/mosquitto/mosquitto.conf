# Config mínima de dev do broker Mosquitto.
# Anônimo liberado para a bancada local; em produção, habilitar auth (MQTT_USERNAME/PASSWORD).
listener 1883
allow_anonymous true

persistence true
persistence_location /mosquitto/data/
